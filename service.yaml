apiVersion: v1
kind: Service
metadata:
  name: mobileapp
  namespace: default
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: mobileapp
  type: LoadBalancer